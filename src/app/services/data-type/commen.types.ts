export type Banner = {
    imageUrl: string,
    targetId: number,
    url: string
};

export type HotTag = {
    id: number,
    name: string,
    position: string
};

export type SongSheet= {
    id: number,
    name: string,
    picUrl: string,
    playCount: number
};
